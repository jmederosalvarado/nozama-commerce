export type SellerPreview = {
  username: string;
  name: string;
  image?: string;
  rating: number;
};

export type SellerDetails = {
  username: string;
  name: string;
  image?: string;
  rating: number;
};
